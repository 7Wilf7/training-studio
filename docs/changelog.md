# 更新日志

## 2026-05-26 —— 移动端打磨 + AI Coach 数据可靠性

- 顶部覆盖：Training / Races 滚动时上面不再露出活动记录的"缝"
- 训练卡片紧凑指标重排（手机端）：时长统一放第一列，所有类型对齐
- AI Coach **更不容易装睡**：教练每次回复前会**实时拉一次**最新训练数据，并在系统提示词里被明确告知"`[Recent Activities]` 就是你的全部数据源，不要瞎说看不到"
- 账号区新增**修改密码**入口（电脑：右上角账号名展开菜单；手机：Settings tab → Change password）
- 应用图标改为自定义 PNG/JPG（PWA 安装时使用）
- Guide 改写成双端通用版本，不再写"四个顶层 tab"这种 PC 专属说法

## 2026-05-22 —— 初始版本

Training Studio 文档首次发布。覆盖以下内容：

- 训练记录（Run / Strength / HIIT）和表单驱动的录入流程
- 配速子类规则，以及 Road Run 基于心率的自动分类阈值
- Garmin CSV 导入流程，包括未知类型审核和重复检测
- 赛事管理（目标 + 历史），A/B/C 优先级，PR bar 自动聚合
- AI 教练机制 —— prompt 拼装、历史筛选、Memory、计划导入、长对话提示
- 图表 —— 跑步距离趋势、路跑类型分布、心率区间分布
