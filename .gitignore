# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.*
!.env.example

# Vite cache
.vite/

# Claude Code local settings
.claude/settings.local.json

# Local launch script
启动.bat

# Capacitor / Android build outputs (the android/ Gradle project itself
# IS committed — only the build products are ignored)
android/.gradle/
android/build/
android/app/build/
android/app/release/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/capacitor.settings.gradle
android/local.properties
android/*.iml
android/.idea/
*.apk
*.aab
*.keystore
*.jks

# Signing config — references the keystore by path + holds passwords.
# Generated locally; in CI the values come from GitHub Secrets via env vars.
android/keystore.properties

# Claude Code worktrees
.claude/worktrees/

# Supabase CLI local link state
supabase/.temp/
